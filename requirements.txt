# GrabInsurance MCP - Dependencies
# Install with: pip install -r requirements.txt

# Core MCP and AI
mcp>=1.0.0
anthropic>=0.40.0

# FastAPI and async
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0

# Data and database
pydantic>=2.0.0
aiosqlite>=0.20.0
python-dotenv>=1.0.0

# Testing (optional - remove if not needed)
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
